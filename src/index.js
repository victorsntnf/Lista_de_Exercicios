import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Run from './Run';
ReactDOM.render(
  <React.StrictMode>
    <Run />
  </React.StrictMode>,
  document.getElementById('root')
);